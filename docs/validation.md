# 検証記録

検証日: 2026-09-06。Windows / Node.js 24.16.0 / Chrome。長期保存・組織・議事録連携 v2.3を含めて確認。

## 自動検証

- `npm.cmd run check`: TypeScriptの型チェック。
- `npm.cmd run build`: 本番用フロントエンドの生成。
- `npm.cmd test`: 26件すべて成功。v2.3で履歴500件超の保持、削除前のタスクと実行者の保存、組織階層・重複・権限、複数担当者、Google文書の任意更新・改訂保存・失敗時の原文保持、AIによる既存0円の保持、同時編集拒否、処理再開、日次バックアップ・別フォルダー復元・復元後のセッション無効化を検証。既存の認証・タスク・営業・3か月デモも継続検証。
- `node .browser-check.mjs --sales`: 独立した一時DBで、既存タスク管理の初期設定・ログイン・ホーム・カンバン操作・タイムライン・タスク・コメント・承認・社内依頼・メンバー・初回パスワード変更に加え、営業マスタのプレビューと確定、ヨミ・媒体指標・競合継続月数の保存と再読込、商談登録、議事録保存、ローカル要点抽出、既存タスク画面への連携を検証。390px幅でページ全体の横はみ出しがないことも確認。
- Google Sheetsはモック通信でサービスアカウントの署名・読み取り専用スコープ・固定ホスト・エラー処理を検証。Geminiはモック通信で構造化結果と原文の根拠照合、不正な応答と通信エラーを検証。添付の営業データを外部サービスへ送信していません。
- 添付TSVの42列・4アカウントを解析エラーなしでプレビュー可能。参考データを利用中のDBへ自動投入していません。

## 検証の範囲

v2.3のブラウザーでは、組織登録と自己所属、他部署のユーザーが作ったプロジェクトへの参加表示、通常タスクの営業アカウント選択・複数担当保存、履歴閲覧、手動バックアップ、390px幅の表示を確認しました。Google Driveの読み取り専用スコープ・固定ホスト・取得中の変更検出と、Geminiの数値・単位・対象月・根拠検証はモック通信で確認しています。Googleの実接続は未設定です。

利用中のDBはv2.3起動前にバックアップしました。起動後、既存218レコードとユーザー情報に変更がないこと、161件の基準監査レコードが作成されたこと、日次バックアップの状態保存、画面・ヘルスチェック200、未認証の監査・タスク・営業APIが401であることを確認しました。

v2.2では3か月それぞれの目標額、月・会議週の切り替え、6アカウント・6商談・6議事録の表示、実データへの切り替え、390px幅の表示をブラウザーで確認しました。利用中の環境への投入前にバックアップし、元からある9レコードが変更されていないことを確認しています。

v2.1のブラウザー検証では、期限の今日／7日目／8日目の境界、完了除外、優先度・期日順、全角半角と複数語の検索、Ctrl K、未保存変更の保持・破棄、CPA・採用単価の即時計算と0／不明の区別、議事録本文検索も成功しました。画面は目視でも確認しています。

`npm.cmd audit --json` は開発依存も含めて脆弱性0件。公開前チェックで既知の秘密鍵形式・除外対象ファイルを検査しました。v2.1.0のGitHub Actionsは、Linux上のブラウザー検証・Dockerビルドまで成功しています。このPCにDockerはありません。後続コミットの結果はGitHubの「Check Worknest」で確認できます。

実Google Sheets／Geminiへの接続は資格情報が未提供のため未検証。実アカウントでのGoogle予定保存・Drive共有権限、外部サーバーへのクラウド配置、Docker実行、大規模負荷も未検証。Sheetsは読み取り専用、Calendarは利用者が予定作成を確定するリンク連携です。設定と運用の詳細は [営業管理ガイド](sales-guide.md) を参照してください。
